require 'spec_helper'

describe AppSpiderRails do
  it 'has a version number' do
    expect(AppSpiderRails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
